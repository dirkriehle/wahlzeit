language: bash

stages:
  - build
  - name: deploy
    if: branch = master

services:
  - docker

jobs:
  include: 
    - stage: build
      script: docker build -t wahlzeit .
    - stage: deploy
      script: echo 'deploy stage stub'